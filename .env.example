# Copy to .env and fill in locally. Never commit .env.

# Port for the local preview server (server.js)
PORT=3000

# Bearer token required by the admin report API (public/admin/api/sponsor-reports.js)
ADMIN_API_TOKEN=
